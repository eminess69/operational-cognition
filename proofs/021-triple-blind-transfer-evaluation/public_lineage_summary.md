# Public Lineage Summary

This proof uses public-safe local artifacts only:

- five held-out task prompts
- anonymized answer files
- locked blind scores
- hidden mapping revealed after the blind score lock
- post-reveal score computation

The blind scorer saw no source refs and no production mode names. MCP-seasoned refs appear only in `mode_mapping_hidden.json`, which is explicitly excluded from scoring inputs until after `blind_scores.json` is locked.

Publicly supportable result: In this bounded five-task blind evaluation, the MCP-seasoned condition scored higher than both controls after reveal.

Unsupported claims remain outside scope: broad capability, target-system diagnosis, endpoint expansion, reusable-pool mutation, and candidate promotion.
