# Blind Pack

`blind_inputs.json` contains visible case metadata and submitted reconstruction keys only. It does not contain expected outcomes. `blind_expected_hidden.json` contains the hidden expected reconstruction keys used by the scorer.

`blind_scorer.py` scores key overlap and does not read scenario narrative claims or comparative reports.

