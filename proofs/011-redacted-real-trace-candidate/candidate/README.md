# Candidate Data

No candidate trace data is stored here yet.

Only public-safe converted candidate artifacts may be added to this directory after source selection and redaction review. Keep this directory empty while `candidate_status` is `not_selected`.
